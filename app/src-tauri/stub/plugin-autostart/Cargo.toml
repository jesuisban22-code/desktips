[package]
name = "bureau-stub-plugin-autostart"
version = "0.1.0"
edition = "2021"

[lib]
name = "tauri_plugin_autostart"
path = "src/lib.rs"

[dependencies]
tauri = { path = "../tauri", package = "bureau-stub-tauri" }
